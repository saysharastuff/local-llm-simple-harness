name: Format context benchmark

on:
  push:
    branches:
      - decision-engine-integration
    paths:
      - ".github/workflows/format-context-benchmark.yml"

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: decision-engine-integration
      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npx prettier benchmark/context-packing/manifest.json benchmark/context-packing/run.js --write
      - name: Commit formatted files
        run: |
          if git diff --quiet; then
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add benchmark/context-packing/manifest.json benchmark/context-packing/run.js
          git commit -m "Format context packing benchmark"
          git push origin HEAD:decision-engine-integration
